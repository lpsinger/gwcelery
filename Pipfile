[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[packages]
# Our package and all of its dependencies.
gwcelery = {editable = true, path = "."}
# Include IPython for a nicer ``gwcelery shell`` experience.
ipython = "*"
# Include IPython kernel for Jupyter Lab support.
ipykernel = "*"
# flower >= 0.9.5: https://github.com/mher/flower/pull/880
# flower < 1: flower >= 1 requires Celery 5. FIXME: remove constraint after upgrade to Celery 5.
flower = ">= 0.9.5, <1"
# Dependencies needed for running unit tests
pytest-flask = "*"
pytest-socket = "*"
