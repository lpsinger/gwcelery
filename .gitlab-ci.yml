image: ligo/builder:el7

variables:
  PIP_CACHE: "pip-cache"

cache:
  paths:
    - /var/cache/yum
    - $PIP_CACHE

stages:
  - test
  - deploy
  - mirror

test:
  coverage: '/^TOTAL\s+.*\s+(\d+\.?\d*)%/'
  script:
    - echo keepcache = 1 >> /etc/yum.conf
    - yum install -y python-pip pyxmpp lalinference-python lalsuite-extra
    - mkdir -p "$PIP_CACHE"
    - pip install --cache-dir="$PIP_CACHE" --upgrade pip setuptools pytest-cov pytest-runner mock
    - pip install --cache-dir="$PIP_CACHE" .
    - python setup.py test --addopts='--cov --cov-report=html --cov-report=term'
  artifacts:
    paths:
      - htmlcov/

mirror:
  script:
    - echo $GITHUB_DEPLOY_KEY > ~/.ssh/id_rsa
    - git remote add github https://github.com/lpsinger/gwcelery
    - git push github master

pages:
  stage: deploy
  dependencies:
    - test
  script:
    - mv htmlcov/ public/
  artifacts:
    paths:
      - public
    expire_in: 30 days
  only:
    - master
