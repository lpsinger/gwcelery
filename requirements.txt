astropy
celery[redis] >= 4.2.0
celery_eternal >= 0.0.3
comet
flower
gwpy
healpy
jinja2
lalsuite >= 6.49.1.dev20181212
ligo-followup-advocate >= 0.0.11
ligo-gracedb >= 2.1.1
ligo-raven >= 1.8
ligo-segments
ligo.skymap >= 0.0.18
lscsoft-glue
lxml
numpy
pygcn >= 0.1.16
pytest >= 3.0
redis
safe-netrc
sentry-sdk
service_identity  # We don't actually use this package, but it silences some annoying warnings from twistd.
sleek-lvalert >= 0.0.4
voeventlib >= 1.2
